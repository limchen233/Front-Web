在`ES6`的模板字符串语法中循环渲染数据：

```jsx
if (item.data) {
  var temp = `${item.data.map(items => { 
    return `<input type="checkbox" value="${items.value}" lay-filter="filter" 
               lay-skin="primary" title="${items.label}" />
            </br>`
				}).join('')}`
	return temp
}
```

